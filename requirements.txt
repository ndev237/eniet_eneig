# ============================================================
# Dépendances Python du projet ENIET / ENIEG
# Installation : pip install -r requirements.txt
# ============================================================

# --- Framework ---
Django==6.0.5
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# --- Frontend / Templates ---
django-tailwind==4.4.2
django-tinymce==5.0.0
pytailwindcss==0.3.0

# --- Production : serveur statique + WSGI ---
whitenoise==6.7.0
gunicorn==23.0.0

# --- Base de données PostgreSQL (prod) ---
# psycopg[binary] = wheel précompilée, pas de libpq système à installer
psycopg[binary]==3.2.3

# --- Traitement d'images (OBLIGATOIRE pour ImageField) ---
# Sans Pillow, tout upload d'image (album, article, photo) renvoie HTTP 500.
Pillow==11.0.0

# --- Variables d'environnement ---
python-dotenv==1.0.1

# --- Utilitaires ---
arrow==1.4.0
python-dateutil==2.9.0.post0
python-slugify==8.0.4
text-unidecode==1.3
PyYAML==6.0.3
requests==2.34.2
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.15
urllib3==2.7.0

# --- Markdown / docs ---
Jinja2==3.1.6
MarkupSafe==3.0.3
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0

# --- CLI / dev tools ---
click==8.4.0
colorama==0.4.6
rich==15.0.0
honcho==2.0.0

# --- Scaffolding ---
cookiecutter==2.7.1
binaryornot==0.6.0
six==1.17.0
